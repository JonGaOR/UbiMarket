---
deployment:
  tasks:
    - export DEPLOYPATH=/home/ubimxghu/UbiMarket/
    - /bin/rm -rf $DEPLOYPATH/*
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH && npm install --production
    - touch $DEPLOYPATH/tmp/restart.txt
